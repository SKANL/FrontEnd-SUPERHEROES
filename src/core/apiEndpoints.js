// Centralización de endpoints de la API
export const API_BASE_URL = 'https://<tu-servidor>/api/v1';
// Ejemplo de endpoints:
export const ENDPOINTS = {
  productos: `${API_BASE_URL}/productos`,
  ventas: `${API_BASE_URL}/ventas`,
  // ...otros endpoints
};
